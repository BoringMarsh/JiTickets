<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--namespace指向mapper.xml所对应的Mapper接口-->
<mapper namespace="cn.edu.tongji.springbackend.mapper.StudentKeywordMapper">
    <resultMap id="studentKeywordResultMap" type="studentKeyword">
        <!-- Add result mappings for each field in the StudentKeyword class -->
    </resultMap>

    <select id="getStudentKeywords" resultMap="studentKeywordResultMap">
        SELECT * FROM student_keyword WHERE stu_id = #{stuId}
    </select>

    <insert id="insertStudentKeyword" parameterType="studentKeyword">
        <!-- Insert statement for student_keyword -->
    </insert>

    <update id="updateStudentKeyword" parameterType="studentKeyword">
        <!-- Update statement for student_keyword -->
    </update>
</mapper>